# Code Review Backlog

Unfixed notes from code review (review agent) runs. When reviewing this backlog, be critical: if a finding has real risk (future breakage, i18n issues, security), create an issue. If it's superficial or speculative, delete it.

---

*Cleared 2026-04-22 during UI Redesign & Student Profile Polish sprint close. Actionable entries batched into #833 (bug batch: todo validation + countdown + difficulty feedback), #837 (deduplication: getInitials/formatRelativeDate/CEFR_ORDER). Remaining entries deleted (verified safe, intentional per spec, already tracked in #737/#707/#644/#714/#715/#716/#683/#741/#742/#756/#809/#657, or consistent with existing patterns).*

*Cleared 2026-04-27 during Student Profile Voice Input sprint close. Actionable entries batched into #989 (DS component polish), #990 (code hardening), #992 (navigation UX). Dismissed and cosmetic entries deleted (#927 dismissed, #947 rename too cosmetic).*

*Cleared 2026-05-11 during Text Correction sprint close. All actionable entries batched into: #1222 (Pass 2 ser/estar enforcement), #1223 (rate limit /corregir), #1224 (arch: prompt builders + stale-recovery), #1225 (frontend: status badge map + chip legend + breadcrumb), #1226 (pedagogy: C category), #1227 (generation grammar scope), #1228 (DB hardening), #1229 (prompt externalization), #1230 (Atelier hardening), #1231 (UI polish), #1232 (test helpers), #1233 (prompt-health sweep). Deleted: cosmetic entries (eslint-disable style, timer pattern, FAB doc placement, readme-only concerns). Dismissed: intentional accessibility trade-offs (aria-disabled FAB), intentional model clarity choices (ser/estar duplication), code patterns with no current bug.*

## Hardening II (2026-05-16)

| PR | Reviewer | Finding | Action |
|---|---|---|---|
| #1263 | Prompt-health | `grammarOutOfScope` adds noise to generation prompts when `grammarFocusTargets` is populated -- exclusions redundant for generation since positive list suffices. Could omit OutOfScope from BuildGrammarScopeBlock when GrammarFocusTargets is populated. | Deferred: minor; generation prompts benefit from explicit exclusions as safety net for over-eager models |
| #1263 | Sophy | Add startup validation that `grammarFocusTargets` entries are a subset of `grammarInScope`. Prevents future authors from adding active targets outside the receptive scope. | Deferred: low risk currently (single level defines this field); track for next pedagogy hardening sprint |
| #1279 | Arch | `OCR_` error code prefix and `OcrDtos.cs` / `OcrOptions.cs` / `OcrException.cs` names remain after renaming controller to `FileTextExtractController`. Split identity: public route says `extract-text` but internal types say OCR. No functional impact. | Deferred: rename types in a follow-up to avoid sprawl; not blocking |
| #1286 | Arch | `SanitizeForPrompt` is duplicated across `RedaccionScopeAffirmerPromptBuilder` and `RedaccionLevelFilterPromptBuilder`. Should be extracted to a shared static helper in the AI namespace. | Deferred: minor code smell; fixed the `\n` divergence in this PR; refactor to shared helper tracked here |
| #1286 | Sophy | `NextLevel` dictionary in `RedaccionScopeAffirmerPromptBuilder` hardcodes CEFR chain in C#. Should be derived from IPedagogyConfigService.GetNextLevel(cefr) or similar. | Deferred: CEFR 6-level chain is stable and unlikely to change; extract when IPedagogyConfigService is next extended |
| #1286 | Sophy | `wordCount > 800` cost guard is a magic number; belongs in app settings / pedagogy config alongside other pipeline knobs. | Deferred: minor; acceptable for v1 |
| #1286 | Sophy | Spanish praise template `"¡Bien hecho! Usaste {label}..."` is hardcoded user-visible copy in service code. Should live in a localization/template file. | Deferred: acceptable for v1; create localization issue if multi-language support is added |
| #1293 | 2026-05-17 | LOW | `RedaccionDetail.tsx` slow-hint timers use `useEffect`+`setTimeout` cleanup; other components (AtelierAssistantPanel, FullLessonGenerateButton, LessonNotesCard) store handles in `useRef` and cancel imperatively. Functionally correct; convention mismatch. |
| #1301 | 2026-05-17 | LOW | `BuildSectionConversationPrompt` (WarmUp/WrapUp) omits `BuildGrammarScopeBlock`. Intentional per spec (icebreaker content uses `GetScopeConstraint` instead). Log here if intent changes to full ceiling coverage across all conversation types. |
| #1301 | 2026-05-17 | LOW | Sophy: four sibling methods (ConversationUserPrompt, ExercisesUserPrompt, GrammarUserPrompt, LessonPlanUserPrompt) each manually remember to call BuildGrammarScopeBlock + BuildWeaknessTargetingForSection + templateGuidance etc. A small `AppendStandardBlocks(sb, ctx, level)` helper would make the next omission impossible. Deferred; not in scope for this XS fix. |
| #1319 | 2026-05-19 | LOW | Arch: `ClaudeApiClient.CompleteWithToolAsync<T>` does not return token counts to callers (only T). Token data is logged inside the method via EmitCallLog with correlationId -- still queryable in Log Analytics by correlationId. The "Background correction completed" summary log no longer includes ModelTokens. Create follow-up if this causes monitoring gaps. |
| #1319 | 2026-05-19 | LOW | Arch: `ClaudeApiClient` introduces a new `JsonSerializerOptions` instance -- 7th copy in the codebase. Pre-existing pattern. Consolidate if a shared `AppJsonOptions` utility is ever added. |
| #1326 | 2026-05-23 | LOW | Arch: DashboardService GetActiveStudentsAsync projection block inlines the `(sl.StudentId == s.Id OR sl.GroupId IN student.Groups)` predicate five times across LastSessionDate / NextSessionDate / TotalSessions / CancelledSessionsLast30Days / LastHomeworkStatusRaw. The canonical `SessionLogQueries.ForStudentIncludingGroups` helper can't be called inside an EF Core projection that correlates with the outer row's `s.Id`. Solution would be an `Expression<Func<SessionLog, bool>>` builder injected at projection-build time. Deferred -- correctness is fine, only the duplication smells. |
