# Observed Issues

Out-of-scope observations logged by agents during implementation. Each row is something an agent noticed but did not fix because it was outside the current task's scope. These get batched into future GitHub issues by the PM.

| Source issue | Date | Severity | Observation |
| #1219 | 2026-05-10 | medium | Portuguese L1 additionalNotes "Do not apply the romance family ser-estar contrastive note" causes model to skip ser/estar error detection entirely. Should be reworded to "do not add an L1 contrastive explanation" not "skip detection". |
| #1219 | 2026-05-10 | medium | OFFSETS section "CORRECT procedure" steps 1-4 cause model to generate 6000-15000 tokens of post-JSON verification work. Should be reframed as "internal reasoning only, output nothing". Drives MaxTokens up and slows corrections significantly. |
| #1203 | 2026-05-10 | low | Sophy: muybien note (warm praise text) is generated by Pass 2 but discarded because CorrectionTag has no note column. Either persist the note or drop note generation from the prompt. Requires schema migration. |
| #1203 | 2026-05-10 | low | Sophy: sanitization asymmetry -- Pass 1 uses InputSanitizer.Sanitize, Pass 2 uses SanitizeForPrompt for the same AssignmentPrompt field. Should unify at the service boundary. |
| #1203 | 2026-05-10 | low | Sophy: ser/estar worked examples and register-mismatch examples are hardcoded in C# SystemPrompt literals. Long-term they belong in JSON config alongside grammar scope data. |
| #1224 | 2026-05-11 | low | Prompt health: RedaccionCorrectionPromptBuilder "NEVER G / NEVER L / NEVER G" disambiguation rules stated as negatives. Could be rewritten as positive statements for clarity. |
| #1224 | 2026-05-11 | low | Prompt health: spannedText uniqueness rule stated twice in RedaccionCorrectionPromptBuilder (in the offset procedure and as a standalone bullet). Minor duplication. |
| #1225 | 2026-05-11 | low | RedaccionDetail has no test file; the new getStudent query in the breadcrumb is untested. Acceptable for a polish task but worth adding a test in a hardening pass. |
| #1203 | 2026-05-10 | low | Prompt health: redundant verbatim-echo rationale appears twice in Pass 1 prompt (line 84 and line 98 paragraph). Minor cleanup opportunity. |
| #1192 | 2026-05-09 | low | No dedicated test asserting `refetchInterval` is 3000ms when Corrigiendo cards exist -- only behavior-level coverage via component test |
| #1207 | 2026-05-10 | low | Arch: STATUS_BADGE (CSS) and status label are split across two Record maps in RedaccionesTab.tsx; RedaccionDetail.tsx has its own parallel palette Record. Should unify into a shared constants file as statuses grow. |
| #1207 | 2026-05-10 | low | Code: CorreccionFallida failure tests use Task.Delay(500) instead of the existing WaitForDbStatusAsync polling helper; could be flaky on slow CI. |
| #1207 | 2026-05-10 | low | Code: RedaccionDetail.tsx Reintentar button missing data-testid (inconsistent with RedaccionesTab.tsx pattern). |
| #1192 | 2026-05-09 | low | Corrigiendo badge with amber pulse dot not visually verified (no seeded Corrigiendo correction in Ana Visual's data; would need a dedicated seed) |
| #1185 | 2026-05-09 | low | Atelier session picker (inline pick-list inside AI proposals area) has no design system spec; picker rows use ghost-button style with hover-bg but no selected/active state defined -- needs Vera discussion before pattern is reused |
| #smoke-text-correction | 2026-05-09 | low | Atelier assistant extracted phantom profile data (profession "Engineer", country "Spain", interests "Travel, Cooking") from a session note that contained none of that information -- extraction model may be hallucinating updates from prior context rather than the current transcript |
| #1181 | 2026-05-09 | low | CorrectionDrawer footer pairs "Cancelar" ghost button with primary "Guardar" on the same row; violates design-system §5 (destructive/secondary actions should be separated from primary). Pre-existing, not introduced by this PR. |
| #1151 | 2026-05-09 | P3:nice | Apply-disabled + "Open from a student's screen" helper text invariant has no e2e coverage; was in the no-student spec but can only be tested from a student context (panel open) -- needs a new spec |
| #smoke-hardening | 2026-05-04 | medium | `/dashboard` route renders blank white page; correct route is `/` - sidebar link uses `/` but direct navigation to `/dashboard` silently fails |
| #1065 | 2026-05-04 | low | CreateTeachingTodoDto.DueDate lacks [RegularExpression] format attribute; invalid dates accepted at model-binding boundary and silently become null (Sophy) |
| #1065 | 2026-05-04 | low | Todo trigger phrases ("añade un teaching todo", etc.) duplicated in two PromptService.cs blocks; should be extracted to data/atelier/intent-triggers.json in a future cleanup (Sophy) |
| #1065 | 2026-05-04 | low | CreateTeachingTodoDto.DueDate is string? while sibling CreateTeacherFollowupRequest.DueDate is DateOnly?; inconsistent binding strategy for same entity field (arch review) |
| #1165 | 2026-05-09 | low | AssistantProposeRequest uses `SessionId` but AssistantProposeResponse uses `SessionLogId` for the same entity; naming asymmetry in the API contract (Sophy) |
| #smoke-hardening | 2026-05-04 | low | "Apply" button in Atelier Assistant proposal flow not smoke-testable without real audio; #1065 fix end-to-end unconfirmed |
| #1231 | 2026-05-12 | low | Arch reviewer noted AtelierAssistantPanel mic-retry-btn (line 377) uses indigo while Keep editing uses zinc; these are different UI states (mic error vs discard confirm) so no inconsistency -- dismissed. |
| #1153 | 2026-05-08 | low | `frontend/src/pages/StudentForm.test.tsx > "includes identity fields in form submission"` is flaky under parallel vitest load (1 fail in full run, 72/72 pass when isolated). Pre-existing, unrelated to redacciones. |
| #1155 | 2026-05-08 | low | CorrectionDetailDto has no studentTextUpdatedAt field; stale-markup banner (issue #1155 edge case) deferred until DTO grows that field (plan revision) |
| #1155 | 2026-05-08 | low | DemoSeeder Ana Visual correction duplicates marked-up JSON literal and tagSpecs array; could be serialized once from tagSpecs (Sophy) |
| #1155 | 2026-05-08 | low | Visual stack DB_PORT=1435 is not in correction-detail.visual.spec.ts .env; pre-existing infra pattern across visual specs (review-ui) |
| #1154 | 2026-05-08 | low | StudentRoster.test.tsx fails 3 tests on sprint/text-correction head (Review pending / HMWK NOT DONE / HMWK PARTIAL); pre-existing on sprint, unrelated to redacciones; landed via #1154 merge |

*Cleared 2026-04-22 during UI Redesign & Student Profile Polish sprint close. Actionable entries batched into: #833 (bug batch), #834 (seeder gaps), #835 (e2e session-log rewrite), #836 (ScenarioSeeder Hans B1), #837 (deduplication), #838 (session title from web UI), #839 (debug log privacy), #840 (Edit Student UX), #841 (stale closure + LogSession pre-populate). Already-tracked entries removed (referenced #737/#707/#644/#714/#715/#716/#683/#741/#742/#756/#809/#657). Dismissed entries removed (defensive-only, intentional, or resolved).*

*Cleared 2026-04-27 during Student Profile Voice Input sprint close. Batched into: #989 (DS polish), #990 (code hardening), #991 (e2e fixes), #992 (navigation UX), #993 (infra). Already-tracked entries deleted (#874, #860, #861, #869, #870, #871, #880 already had issues). Dismissed and cosmetic entries deleted (intentional patterns, pre-existing with no risk escalation). Smoke-test dismissed entries removed.*

*Cleared 2026-05-03 during Unified Voice & Chat sprint close. Batched into: #1064 (Vera DS canonicalization, Hardening), #1065 (Atelier extraction intent leakage, Hardening), #1066 (dedup and config-extraction sweep, Hardening), #1067 (standalone hardening batch, Hardening), #1063 (/help broken link, current sprint, merged). Already-filed: #1059 (e2e port 5000) moved to Hardening. Deleted: smoke-test new-student apply 400 (fixed by #1058); #992-arch SessionHistoryTab text-sm (intentional per #992 spec); #1056 useEffect-based state reset pattern (speculative, not investigated, no concrete bug); #1004 STT confidence regions (architectural backend gap, will resurface organically when needed).*

*Cleared 2026-05-07 during Hardening sprint close. Filed: #1146 (/dashboard blank route, bug, Text Correction milestone), #1147 (atelier extraction config externalization + naming alignment, Text Correction), #1148 (frontend post-Hardening polish sweep, Text Correction), #1149 (backend + e2e + ops post-Hardening cleanup, Text Correction). Deleted as already shipped via closed Hardening tasks: #994-sophy x3 and #994-security (shipped via #1066/#1067), #990-arch useMemo (rolled into #1148), #990-sophy PromptService strings (rolled into #1149), #992-arch nav pattern (shipped via #1067), #992-arch text-sm (intentional per spec), #1031-ui FAB DS gap (shipped via #1064), #1049-ui db-helper email (shipped via #1083), #1051 useAtelierAssistant cancellation (shipped via #1067), #1056 useEffect speculative (not investigated, no concrete bug), #1075 bicep double-prefix (shipped via #1084), #smoke Apply on new-session form (shipped via #1076), PM-2026-05-03 LangTeach rebrand (shipped via #1078), #smoke-hardening Apply smoke gap (observational only, e2e covers it).*

*Partial batch 2026-05-09 (PM session). Filed: #1175 (prompt hardening accented char offset), #1176 (Vera DS spec for correction annotation patterns), #1177 (tech-debt sweep: naming, architecture, prompt openers). Deleted entries batched into #1175: #1166 accented char offset. Batched into #1176: three #1155 DS gaps (status pill, C/G/L/O chip, muy bien). Batched into #1177: #1065 DueDate x2, #1065 trigger phrase dedup, #1071 formatTime/formatTimer, #1078-arch x2, #1111 AssistantController DB, #1132 sessionFieldValues + naming, #1149 x3 Isaac prompt openers, #1156-arch filename sanitizer, #1156 docx color map. Dismissed: #1082 CEFR_SUBLEVELS (intentional feature, not a bug), #1113 review-ui worktree limitation (documented behavior, not actionable).*

| #888 | 2026-05-04 | low | session-log-voice.spec.ts still uses getByRole('tab', { name: /history/i }) -- same selector that was breaking the nightly session-log tests. Not in nightly suite so not blocking, but will fail if added. Should be updated alongside next session-log-voice work. |
| #888 | 2026-05-04 | low | Tests finding seeded students (Ana Visual, Diego Seed) use pageSize=100 to work around pagination -- fragile if visual seed roster grows past 100. A findStudentByName() helper that handles pagination would be more robust. |
| #1082 | 2026-05-04 | low | LogSession.tsx has `CEFR_SUBLEVELS` array with dot-notation levels (A1.1, B1.2 etc.) for per-skill tracking UI. This is an intentional feature but the sublevel values are stored in DB. Future cleanup: decide if this should use base levels only. |
| #1113 | 2026-05-05 | low | review-ui agent cannot test worktree changes: Docker e2e stack uses `context: .` (main repo), so worktree diffs are invisible to it. Review-ui is only reliable post-merge or when run from main repo. |
| #1154 | 2026-05-08 | low | StudentForm.test.tsx (and a couple of its peers) intermittently fail under task-build-verify's parallel load with `pointer-events: none` on radix-ui Select dropdowns and 5s timeouts. Pass 72/72 in isolation. Pre-existing flake exposed by CPU contention; not introduced by this task. Consider raising vitest testTimeout for the heavy form tests, or marking them serial. |
| #1194 | 2026-05-10 | low | CorrectionDocxExportServiceTests.cs and CorrectionsControllerExportTests.cs have pre-existing CS8602 nullable dereference errors (lines 22, 96, 216 in service tests; line 139 in controller tests). dotnet build -warnaserror fails on these. Pre-existing before this task; not introduced here. |
| #1205 | 2026-05-10 | low | AssistantController.cs:225 `$"{sd}T{st}"` concat assumes YYYY-MM-DD and HH:mm format -- implicit contract; Sophy suggests a comment |
| #1215 | 2026-05-10 | low | StripFencesAndPreamble added to ContentJsonHelper for Pass 1 correction responses but CurriculumGenerationService, ReflectionExtractionService, StudentProfileExtractionService still call StripFences on JSON-object responses. These are equally exposed to prose-preamble from the model. Follow-up: migrate to StripFencesAndPreamble. |
| #1215 | 2026-05-10 | low | Scenario 5 (register rule): Pass 1 correctly generates L for te invitaran (informal letter register mismatch). Level filter promotes to MuyBien for B2 students because imperfect subjunctive is in-scope for B2 (todos los usos). Explicit prohibition in filter prompt is overridden by the positive grammar-scope signal. Pre-existing level filter behavior for in-scope structures; not introduced by this task. |
| #smoke-text-correction | 2026-05-11 | minor | Clicking the card title/body on the Redacciones list does not navigate to the detail view -- only the ">" chevron works. Editar also opens the edit panel, not a read view. Consider making the whole card row clickable for standard list UX. |
| #smoke-text-correction | 2026-05-11 | minor | B1 correction of a 120-word letter flagged only G-category errors (2 spans). No C, L, or O chips generated on test text. May be correct AI output for simple input; worth verifying C/L/O appear with real student errors containing misspellings or cohesion gaps. |
| #smoke-text-correction | 2026-05-11 | minor | "Editar" action on a CORREGIDA card opens the same edit form as PENDIENTE -- student text is editable after correction is done. Intentional behavior should be confirmed; locking the text field on CORREGIDA cards would prevent accidental edits. |
| #smoke-text-correction | 2026-05-11 | minor | /atelier direct URL renders a completely blank page (no nav, no content). Not a blocker but could mislead if typed directly. Atelier is only accessible via FAB. |
| #review-ui-sprint-text-correction | 2026-05-11 | minor | REFUTED: review-ui-sprint flagged "Log Session" button clipped at 375px mobile. Code check confirmed header files were not changed in this sprint -- pre-existing issue, not a sprint regression. |
| #review-ui-sprint-text-correction | 2026-05-11 | minor | Atelier new-session proposal card date input uses native browser format (mm/dd/yyyy placeholder) while the rest of the UI uses shadcn/ui date pickers. Minor inconsistency. |
| #1222 | 2026-05-11 | minor | IsSerEstarGTag heuristic hardcodes ser/estar as a taxonomy rule in C#. Sophy flagged: 'always-keep G subcategories' should be driven from a JSON config (e.g. data/correction/always-keep-grammar-rules.json) so adding preterite/imperfect, por/para etc. doesn't require C# changes. Deferred to a future hardening task. |
| #1223 | 2026-05-11 | medium | Arch: /corregir does not call IUsageLimitService.CanGenerateAsync / RecordGenerationAsync, so corrections bypass the per-teacher monthly generation quota that GenerateController enforces. Rate limiter added here is anti-abuse only (20/hr); the monthly budget is a separate, unguarded path. Needs a follow-up issue. |
| #1237 | 2026-05-11 | minor | OcrController: after a successful blob upload, an OCR failure leaves an orphan blob in the corrections container. Each failed retry leaks one blob. No lifecycle/TTL policy currently set on the corrections container. Acceptable for v1 but should be addressed with a cleanup job or blob TTL. |
| #1237 | 2026-05-11 | minor | Correction.SourceImageUrl stores the SAS URL (expires 15 min). The image is never rendered in the UI (per spec), so this does not affect users. If image preview is added in future, persist the blob path instead and regenerate SAS on read. |
| #1229 | 2026-05-12 | minor | Arch (false-positive): LessonNotesController was flagged as having a third Conflict shape but does not exist in the worktree. Only two controllers use Conflict and both now use { code, message }. No action needed. |
| #1229 | 2026-05-12 | minor | Arch: StripFences partial migration flagged -- other callers (CurriculumGenerationService, LessonService, ContentValidationService, PdfExportService) use StripFences on array responses or structured content, not JSON-object responses. StripFencesAndPreamble searches for '{' only and is not appropriate for '[...] responses. Intentionally left on StripFences. |
| #1229 | 2026-05-12 | minor | Prompt-health: long-term, consolidate CEFR calibration guidance into either the system prompt GUIDANCE block or the per-level cues -- not both. Current duplication is low-risk (cues are additive, not contradictory) but could diverge on future edits. |
| #1258 | 2026-05-13 | low | OcrController fallback dispatch (first extractor throws OcrFallbackException, second returns text) has no integration test. Adding a custom WebAppFactory configuration with two chained extractors would cover it. Current coverage: unit tests on PdfTextExtractor + code review of foreach loop. |
| #1258 | 2026-05-13 | low | Live verify of typed-PDF fast-path (no Vision log line) could not be run pre-merge: e2e stack uses context: . (main repo) and worktree code is invisible. Validate post-merge by uploading a typed PDF and confirming no AzureVisionTextExtractor log line in the API logs. |
| #1232 | 2026-05-12 | low | corrections-sprint.spec.ts moat assertion (countDiffers OR distributionDiffers) is intentionally soft per issue AC; could be tightened to directional assertion (e.g. A2 has more low-level tags) in a future hardening pass |
| #1233 | 2026-05-12 | low | Arch: BuildPracticeSectionPrompt and BuildProductionSectionPrompt fetch GetGuidance directly but never call GetRawForbiddenExerciseTypes -- forbidden-reasons enforcement is asymmetric vs BuildSectionConversationPrompt. Pre-existing; not introduced by this PR. Track if forbidden-reasons is ever extended to practice/production paths. |
| #1263 | 2026-05-16 | medium | Live verify of B1 correction (cuando+subjuntivo not softened) and A1 soften behavior could not be completed pre-push: no existing correction e2e tests and Auth0 required for live API calls. Validate post-merge by submitting a 100+ word B1 redaccion with correct cuando+subjuntivo to confirm no soften/remove tag on that construction. |
