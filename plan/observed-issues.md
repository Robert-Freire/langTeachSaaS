# Observed Issues

Out-of-scope observations logged by agents during implementation. Each row is something an agent noticed but did not fix because it was outside the current task's scope. These get batched into future GitHub issues by the PM.

| Source issue | Date | Severity | Observation |
| #833 | 2026-04-22 | low | `e2e/.env` does not set `VITE_API_BASE_URL`; start-visual-stack.sh comment documents port 5178 but env file does not reflect it. Pre-existing gap. |
| #724 | 2026-04-14 | info | CodeRabbit #739 flagged dual-casing risk in mentionedDifficultyPairs. Dismissed: SerializePairs uses CamelCaseOptions so both API and seeder produce lowercase keys. No mixed-casing rows exist. |
| #724 | 2026-04-14 | medium | No seeded student has SkillLevelOverrides set; Skill Imbalance Analysis always shows empty state in visual review. Consider adding skill overrides to Ana Visual or Ana Seed for visual QA coverage. |
| #721 | 2026-04-14 | low | Ana Seed has no SkillLevelOverrides in seeder so horizontal Skill Assessment badges on Profile tab cannot be visually verified. Consider adding skill overrides to Ana Seed seeder entry. |
| #721 | 2026-04-14 | low | Ana Seed has PersonalNotes but no TeachingNotes in seeder; Teacher's Working Memory dark section only shows Sensitivities subsection. Consider adding TeachingNotes to Ana Seed for fuller visual verification. |
| #674 | 2026-04-12 | low | review-ui agent could not find `log-session-button` testid on student detail page in create mode. Pre-existing selector mismatch, unrelated to this PR. Should be investigated and fixed in a future task. |
| #723 | 2026-04-14 | low | suggestTopicTags keyword list has both "imperfecto" (Spanish) and "imperfect" (English) as separate entries, causing two chips to appear when the narrative contains "imperfecto" (since "imperfect" is a substring). Consider deduplicating Spanish/English variants into a single canonical label. |
| #729 | 2026-04-14 | low | LogSession create mode pre-populates actualContent from plannedForToday during render (outside useEffect), inconsistent with StudentForm pattern using useEffect. Pre-existing pattern; not introduced by this PR. |
| #729 | 2026-04-14 | info | docs/architecture-model.md routes table does not include the new `/students/:id/sessions/:sessionId/edit` route. Minor doc gap. |
| #626 | 2026-04-10 | medium | DemoSeeder uses PersonalNotes as seed-detection marker; if teacher edits that field the seeder loses track and may create duplicates. Pre-existing pattern (was Notes before this PR). Should use a dedicated IsDemo flag or deterministic seed name matching. |
| #636 | 2026-04-10 | low | All controllers use `Auth0Id is null` check instead of `IsNullOrWhiteSpace`, and none wrap `UpsertTeacherAsync` in a try/catch for `InvalidOperationException`. Pre-existing pattern across 16 controllers. Could be addressed in a future controller hardening sweep. |
| #638 | 2026-04-10 | low | NextSessionHero countdown ("IN 35 MIN") is computed once at render time with no interval refresh. Countdown will become stale until the user navigates away and back. Could be improved with a 60-second setInterval in a dedicated hook. |
| #628 | 2026-04-12 | low | CourseService re-does DeserializeListWithStringFallback + flatten for LearningGoals instead of reusing a shared helper. Deferred as #707. |
| #638 | 2026-04-10 | low | Two CEFR badge implementations now exist: legacy getCefrBadgeClasses() (emerald/indigo/purple, pill) and new Stitch CefrBadge component (blue/indigo/warm, square). Deferred unification tracked in #644. |
| #639 | 2026-04-10 | low | StudentProfileOverview.tsx is now dead code (superseded by StudentProfileTab). Should be deleted along with its test file. |
| #639 | 2026-04-10 | low | ui/tabs.tsx (shadcn Tabs component) is no longer imported anywhere. All tab UIs now use custom button-based tabs. Could be removed. |
| #639 | 2026-04-10 | low | toggleDifficultyStatus mutation onError only logs to console with no user-visible feedback. Pre-existing behavior from original StudentDetail. |
| #663 | 2026-04-12 | low | CodeRabbit: langCode() does exact-key lookup -- could produce wrong ISO code for lowercase language names (e.g. 'portuguese' -> 'PO'). Dismissed: all language names come from controlled dropdowns with canonical casing. Case-insensitive fallback would be defensive but adds complexity for a scenario that cannot occur in practice. |
| #653 | 2026-04-11 | medium | difficulty-taxonomy.json `competencies` list should replace Fluency with Interaction + add Mediation per CEFR 2020 Companion Volume; consider adding `critical` severity. Deferred to #657. |
| #653 | 2026-04-11 | low | ReflectionExtractionService.ExtractAsync anchors relative-date resolution to UTC (DateTime.UtcNow). Teachers west/east of UTC could get wrong day around midnight. Pre-existing behavior; needs timezone-aware clock injection. |
| #676 | 2026-04-11 | low | ReflectionExtractionService.ParseResponse logs full raw Claude JSON at Debug level ("Unparseable Claude response"). Pre-existing behavior; could leak session/student content in debug logs. Consider truncating or hashing the payload. |
| #657 | 2026-04-11 | low | DemoSeeder.cs:305 seeds `"competency":"Reading"` which is not in difficulty-taxonomy.json. Seeder bypasses validation so no runtime error, but demo data drifts from taxonomy. Logged as #683. |
| #672 | 2026-04-12 | low | review-ui noted: start-visual-stack.sh uses DB_PORT=1435 but e2e runner defaults to 1434. Running visual specs requires `DB_PORT=1435` explicitly. Consider documenting in the visual stack instructions or playwright.config. |
| #682 | 2026-04-12 | low | TopicTags in older session logs (e.g. DemoSeeder Diego Seed) stores plain string arrays `["grammar"]` instead of object arrays `[{"tag":"grammar"}]`. Both formats coexist. RecentSessions normalizes both at read-time, but the inconsistency may surface elsewhere. Consider a migration or backend normalization on read. **parseTopicTags normalized in #703; display issue resolved.** |
| #677 | 2026-04-12 | low | Session card title shows "Session, Apr 2" fallback when `title` field is null. Title is only populated by AI via Telegram; the LogSession form never sets it. Sessions logged via the web UI will always show the date fallback. Consider adding an optional Title field to the log form. |
| #678 | 2026-04-12 | low | CEFR_ORDER (A1=1..C2=6) is defined independently in ProgressDashboard.tsx, StudentOverviewTab.tsx (array), and potentially other components. Should be extracted to a shared lib utility to avoid drift. |
| #678 | 2026-04-12 | info | CodeRabbit suggested removing % from cancellation rate display. Dismissed: AC says "number only (no trend dot)" meaning no indicator dot/label; the % sign is part of the number representation. Stitch design shows "7%". |
| #725 | 2026-04-14 | low | useStudentAutosave.test.ts uses vi.mock for updateStudent; other hook tests use MSW. Pattern divergence. Deferred to #741. |
| #727 | 2026-04-14 | medium | e2e/tests/session-log.spec.ts references session-log-dialog and submit-session-log test IDs that no longer exist - log-session-button navigates to the full-page LogSession route, not a dialog. Tests were pre-existing failures before this task. Need rewrite to cover the full-page autosave flow. |
| #725 | 2026-04-14 | low | useStudentAutosave implements manual retry/status state; could delegate to React Query useMutation instead. Deferred to #742. |
| #682 | 2026-04-12 | low | Pedagogical Profile card: Ana Seed and other demo students have no skillLevelOverrides set, so the card always shows "No skill overrides set" in e2e scenarios. CEFR bars can only be visually verified with manually-created students. Consider adding overrides to Ana Seed in DemoSeeder. |
| #682 | 2026-04-12 | low | formatDateLong/formatMonthYear (and all other formatDate utils) render ISO timestamps in browser local timezone; UTC midnight dates appear a day earlier for west-of-UTC users. Pre-existing pattern across all date formatters in the app. Could be fixed by adding timeZone: 'UTC' to each formatter or adding UTC-pinned variants. |

*Cleared 2026-04-13 during UI Redesign sprint triage. Resolved entries removed (#628 fixed by #707, #630/#682 fixed by #703). Actionable entries batched into #713 (deduplication), #714 (dead code), #715 (DemoSeeder robustness), #716 (timezone). Dismissed entries removed (defensive-only, intentional per spec, already tracked in other issues, or consistent with existing patterns).*
| #775 | 2026-04-17 | low | section-difficulties nested inside section-teaching-goals 2-col grid -- scrollspy nav never independently activates the Difficulties tab on desktop (both share same Y position). Works on mobile. Acceptable UX trade-off; would require re-separating sections to fix. |
| #797 | 2026-04-18 | medium | ScenarioSeeder.SeedScenario6Async fails with NULL constraint violation in ShortTermObjectives (pre-existing). Scenario 6 (Hans B1) cannot be seeded, blocking demo scenarios that depend on it. Needs investigation and fix in a follow-up task. |
| #808 | 2026-04-19 | low | "Other" is in the backend AllowedNativeLanguages but absent from ALL_LANGUAGES in the frontend (only in NATIVE_LANGUAGES). Combobox accepts custom values so no runtime error, but the asymmetry confuses future developers. Tracked in #809. |
| #808 | 2026-04-19 | low | AllowedNativeLanguages has both "Mandarin" and "Chinese (Mandarin)" as separate entries. Both pass validation and resolve to ZH in LANG_TO_CODE. Pre-existing duplication; tracked in #809 (single-source consolidation). |
| #822 | 2026-04-21 | low | Extraction handler for sessionDate/sessionStartTime uses stale closure state for existing values -- if a teacher edits date/time while extraction is in flight, their change may be clobbered. Pre-existing stale-closure limitation affecting all extracted fields ("existing || extracted" pattern also reads stale closure). Proper fix requires refs for all form fields. |
