# Copy this file to .env and fill in the values.
# .env is gitignored — never commit it.
# Used by docker-compose for local development.

# SQL Server SA password — used by the sqlserver container and the api container.
# Source: Bitwarden vault, item "LangTeach Docker SA"
SA_PASSWORD=
